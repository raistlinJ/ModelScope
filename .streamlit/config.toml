[server]
headless         = true
address          = "0.0.0.0"
port             = 8501
maxMessageSize   = 200

[theme]
base                     = "dark"
# Primary accent — cyan that reads "security / terminal"
primaryColor             = "#2dd4bf"
# Canvas and widget backgrounds — GitHub dark neutrals
backgroundColor          = "#0d1117"
secondaryBackgroundColor = "#21262d"
# Text
textColor                = "#e6edf3"
# System font stack (no CDN, CSP safe)
font                     = "sans serif"
