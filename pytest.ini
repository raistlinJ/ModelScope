[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Suppress the harmless "cannot collect TestResult (has __init__)" warning
# from core/preflight.py's dataclass being seen during collection.
filterwarnings =
    ignore::pytest.PytestCollectionWarning
