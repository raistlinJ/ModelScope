[
  {
    "server": "filesystem",
    "name": "file_creator",
    "description": "Create or overwrite a text file, creating parent directories as needed.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "path": {"type": "string", "description": "Absolute or target-relative file path."},
        "content": {"type": "string", "description": "Text to write."}
      },
      "required": ["path", "content"]
    }
  },
  {
    "server": "filesystem",
    "name": "read_file",
    "description": "Read a text file on the target machine.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "path": {"type": "string", "description": "Absolute or target-relative file path."},
        "max_chars": {"type": "integer", "description": "Maximum characters returned (default 20000).", "default": 20000}
      },
      "required": ["path"]
    }
  },
  {
    "server": "filesystem",
    "name": "write_file",
    "description": "Create or completely overwrite a text file on the target machine.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "path": {"type": "string", "description": "Absolute or target-relative file path."},
        "content": {"type": "string", "description": "Text to write."}
      },
      "required": ["path", "content"]
    }
  },
  {
    "server": "terminal",
    "name": "terminal_execute",
    "description": "Execute a shell command on the target machine and return stdout, stderr, and its exit code.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "command": {"type": "string", "description": "Shell command to execute in the target working directory."},
        "timeout_seconds": {"type": "integer", "description": "Execution timeout in seconds (1-120, default 30).", "default": 30, "minimum": 1, "maximum": 120}
      },
      "required": ["command"]
    }
  },
  {
    "server": "terminal",
    "name": "run_nmap_scan",
    "description": "Run a non-destructive nmap scan on the target machine.",
    "inputSchema": {
      "type": "object",
      "properties": {
        "target": {"type": "string", "description": "IP address, hostname, or CIDR."},
        "arguments": {"type": "string", "description": "Safe nmap flags.", "default": "-F"}
      },
      "required": ["target"]
    }
  }
]
